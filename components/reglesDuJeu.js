// reglesDuJeu.js
export const estMouvementValide = (depart, arrivee, plateau) => {
    // Vérifier si c'est en diagonale
    // Vérifier si la distance est de 1 case (marche simple)
    // Vérifier si la case est libre
    return true; // ou false
}

